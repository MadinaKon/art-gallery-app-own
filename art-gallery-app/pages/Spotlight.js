import React from "react";

export default function Spotlight({ image, artist }) {
  //   function pickOneArtPiece(array) {
  //     return array[Math.floor(Math.random() * array.length)];
  //   }
  return <div>Spotlight</div>;
}
